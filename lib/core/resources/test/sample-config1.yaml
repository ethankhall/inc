---
checkout:
  default: crom
  extensions:
    crom: 
      split: /
      exec: curl -s http://api.crom.tech/project/{split_1}/repo/{split_2} | jq .url

integer: 1
'null': null
array:
  - 1
  - 2

---
exec:
  build:
    exec: ./gradlew build